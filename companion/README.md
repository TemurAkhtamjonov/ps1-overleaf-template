# Computational artifact

The project-specific notebook is `notebooks/Temur_PS1_Commitment_Bond_Revised.ipynb`.

Open it in Google Colab and select **Run all**. It requires only standard Python libraries and writes its synthetic benchmark outputs locally in the Colab runtime.

`hf_space/` contains the static source for the interactive Commitment-Bond Trust Game. Its one-shot mode matches the proposal's primary comparison. Its eight-round mode is a separate fixed-order learning visualization, not a causal test.
